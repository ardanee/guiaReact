//Desde la consola, ubicado en el directorio del proyecto ejecutar el siguiente comando:

npm run build

//Con el comando anterior se crea un directorio "build" el cual contiene el deploy que se debe hospedar en el server.
